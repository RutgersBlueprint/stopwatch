import styled from "styled-components";

const NumLabel = styled.div`
  font-style: normal;
  font-weight: bold;
  font-size: 14px;
  line-height: 17px;
  text-align: left;
`;

export default NumLabel;
